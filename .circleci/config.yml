version: 0.1

workflows:
  build:
    jobs:
      - build
        context: citest

jobs:
  build:
    docker:
      - image: nginx:latest 
    steps:
      - checkout
      - setup_remote_docker
      - run:
          name: Build Docker Image
          command: docker build -t mynginx:latest . 
