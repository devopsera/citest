version: 0.1

jobs:
  build:
    context: citest
    docker:
      - image: nginx:latest 
    steps:
      - checkout
      - setup_remote_docker
      - run:
          name: Build Docker Image
          command: docker build -t mynginx:latest . 
